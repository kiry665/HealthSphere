<Window x:Class="HealthSphere.Windows.AuthWindow"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HealthSphere.Windows"
        xmlns:resx="clr-namespace:HealthSphere.Properties"
        mc:Ignorable="d"
        Title="AuthWindow" Height="450" Width="800">
    <Grid Background="#ffdadada">
        <Border Background="White" Margin="20" CornerRadius="20" MinWidth="600" MaxWidth="800" VerticalAlignment="Center" Padding="30">
            <StackPanel>
                <TextBlock FontSize="30" FontWeight="Bold" Margin="0 10">Авторизация</TextBlock>
                <TextBlock FontSize="20" Margin="0 0 0 5">Логин</TextBlock>
                <TextBox Name="LoginTB" materialDesign:HintAssist.Hint="Введите логин" FontSize="20" Margin="0 0 0 10"></TextBox>
                <TextBlock FontSize="20" Margin="0 0 0 5">Пароль</TextBlock>
                <PasswordBox Name="PasswordTB" materialDesign:HintAssist.Hint="Введите пароль" FontSize="20" Margin="0 0 0 20"></PasswordBox>
                <Button Content="Войти" Click="Button_Click"></Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>
